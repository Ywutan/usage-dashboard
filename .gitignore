node_modules/
*.tsbuildinfo

# The built plugin is versioned: `dsh plugin add github:...` installs a git
# tarball and runs no build, so the artifacts it loads must be in the tree.
# The browser sourcemap is not — it is regenerated on every build and would
# grow the history for a debugging aid.
lib/client.js.map
